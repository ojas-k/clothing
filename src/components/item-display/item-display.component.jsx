import React from 'react';
import './item-display.styles.scss';

const ItemDisplay = () => (
    <div>
        <h1> </h1>
    </div>
)


const mapStateToProps = (state,ownProps)=>({
    item:select
})